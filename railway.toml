[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'gunicorn -b 0.0.0.0:${PORT:-8080} app:app --workers 1 --threads 4 --timeout 120'"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
