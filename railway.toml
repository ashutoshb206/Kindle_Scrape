[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "gunicorn -b 0.0.0.0:$PORT app:app --workers 1 --threads 4 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
